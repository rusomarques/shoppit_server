/* eslint-disable no-console */
const categoriesController = {};
// const Category = require('./../models/categoryModel');

categoriesController.getAll = async (req, res) => {
  // do stuff;
  console.log('categoriesController.getAll firing', req, res);
};

categoriesController.getAllItems = async (req, res) => {
  // do stuff;
  console.log('categoriesController.getAllItems firing', req, res);
};

module.exports = categoriesController;
